name: runner2-smoke
on: [push]
jobs:
  smoke:
    strategy:
      matrix:
        n: [1, 2, 3, 4]
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner2-smoke job ${{ matrix.n }} on $(hostname)"
      - run: sleep 8
      - run: echo "OK ${{ matrix.n }}"
